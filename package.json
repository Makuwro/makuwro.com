{
  "name": "makuwiki-client",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://wiki.showrunners.net",
  "dependencies": {
    "html-react-parser": "^1.4.0",
    "next": "^11.1.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "web-vitals": "^1.1.2"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "eslint": "^7.32.0",
    "eslint-plugin-react": "^7.26.1"
  },
  "scripts": {
    "start": "next start",
    "build": "next build",
    "dev": "next dev",
    "predeploy": "npm run build",
    "export": "next export",
    "deploy": "npm run export && cd out && copy \"..\\CNAME\" CNAME && copy NUL .nojekyll && cd .. && gh-pages -d out -t true"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
